mixin Accordion({ className = '', id, parentId, title, mainId = '', dark, suffixText, noPadding, sortable })
  .Accordion(class=`${className}` id=`${mainId}`)
    .Accordion-parent(id=`${parentId}`)
      +Card({ title, accordionId: id, accordionParentId: parentId, dark, suffixText, noPadding, sortable })
        block
    
    